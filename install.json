{
  "commands": [
    {
      "description": "Create virtual environment",
      "shell": "python -m venv env"
    },
    {
      "description": "Activate venv (Windows)",
      "shell": "env\\Scripts\\activate.bat",
      "if": "os == 'windows'"
    },
    {
      "description": "Activate venv (Unix/macOS)",
      "shell": "source env/bin/activate",
      "if": "os != 'windows'"
    },
    {
      "description": "Upgrade pip",
      "shell": "python -m pip install --upgrade pip"
    },
    {
      "description": "Install requirements (Windows)",
      "shell": "pip install -r \"{{api.path}}\\requirements.txt\"",
      "if": "os == 'windows'"
    },
    {
      "description": "Install requirements (Unix/macOS)",
      "shell": "pip install -r \"{{api.path}}/requirements.txt\"",
      "if": "os != 'windows'"
    }
  ]
}
